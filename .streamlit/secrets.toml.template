# ──────────────────────────────────────────────
# Paste this into Streamlit Cloud → App Settings → Secrets
# Replace every placeholder with your actual values from the JSON key file.
#
# Which worklist a clinician sees is no longer set here -- it's keyed off
# the name they type at login (see WORKLIST_BY_CLINICIAN in
# dvt_review_app.py). One deployment/URL serves every clinician.
# ──────────────────────────────────────────────

spreadsheet_id = "YOUR_GOOGLE_SHEET_ID_HERE"

[gcp_service_account]
type = "service_account"
project_id = "your-project-id"
private_key_id = "your-private-key-id"
client_email = "your-service-account@your-project.iam.gserviceaccount.com"
client_id = "123456789"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-project.iam.gserviceaccount.com"

# For private_key: copy the ENTIRE value from your JSON file as-is,
# including the \n characters. Keep it on ONE line in double quotes:
private_key = "-----BEGIN PRIVATE KEY-----\nMIIEv...your key here...\n-----END PRIVATE KEY-----\n"
